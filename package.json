{
  "name": "safe_network_uploader",
  "productName": "Safe Network Uploader",
  "version": "0.0.0",
  "description": "My awesome app",
  "license": "MIT",
  "main": "index.js",
  "repository": "krishnaIndia/dns_manager",
  "author": {
    "name": "krishna",
    "email": "krishnaof1988@gmail.com",
    "url": "maidsafe.net"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . $npm_package_productName --out=dist --ignore=dist --prune --asar --all --version=0.30.0",
    "build-win": "electron-packager . $npm_package_productName --out=dist --ignore=dist --prune --asar --platform=win32 --arch=64 --version=0.30.0"
  },
  "files": [
    "index.js",
    "src/**"
  ],
  "keywords": [
    "electron-app"
  ],
  "dependencies": {
    "electron-debug": "^0.1.1",
    "ffi": "^1.3.2",
    "mime": "^1.3.4",
    "ref": "^1.0.2",
    "ref-array": "^1.1.1"
  },
  "devDependencies": {
    "electron-packager": "^5.0.0",
    "electron-prebuilt": "^0.30.0",
    "electron-rebuild": "^0.2.5"
  }
}
